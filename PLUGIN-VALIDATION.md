# Проверка плагина 0.8.1 — 19.09.2026

- 50/50 локальных unittest: прежний установщик, сборка плагина и облачная привязка.
- Проверка Plugin Creator прошла; все шесть SKILL.md прошли quick_validate.
- На предшествующей сборке 0.8.0 реальный Codex CLI 0.153.4 зарегистрировал отдельный локальный каталог salon-assistant, установил плагин и подтвердил `installed=true`, `enabled=true`, версию и точный локальный источник.
- Повтор установщика 0.8.0 завершился `plugin_installed_verified`, без повторного add; BOOTSTRAP и пользовательские данные сохранились.
- Тестовый рабочий проект содержит только инструкции и system; копий .agents/skills и system/assistant нет.
- Собранный marketplace содержит шаблон `CLOUD-PROJECT-CONTEXT.json`; все шесть навыков маршрутизируют через единый resolver и не требуют локальный `BOOTSTRAP.json` в облачном проекте.
- `scripts/install_plugin.py --prepare-only` собрал отдельный marketplace 0.8.1 с шестью навыками и облачным шаблоном, не регистрируя плагин в Codex и не меняя Google.
- Проверены отказы для чужих/legacy проектов, изменённых исходников, symlink, неизвестного исхода записи; потерянный ответ принимается только после положительного readback.

Установка 0.8.0 проверена на Mac разработчика в отдельном тестовом проекте. Версия 0.8.1 проверена сборкой и локальными тестами; её установка в личный Codex ещё не повторялась. Облачная привязка проверена структурно в сборке и тестах, без реальных Google-записей. Обнаружение навыков/диалог в новой пользовательской задаче, устройство Светланы и её Google-подключения ещё не проверены. Установщик требует Python 3.10+ и официальный Codex CLI с поддержкой plugin. Автоматическая миграция существующих проектов со скиллами в plugin mode не входит в эту версию. Рабочие таблицы и Calendar этим тестом не изменялись. Это не клиентская приёмка.

Этот отчёт не подтверждает публикацию на GitHub или в общем каталоге OpenAI: их статус проверяется отдельно чтением удалённого источника.
